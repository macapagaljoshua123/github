# Frontend

React + TypeScript frontend for the Search API project.

## Scripts

```powershell
npm run dev
npm run build
npm run lint
```

The app calls `http://127.0.0.1:8000` by default. To override it, create `.env.local` in this folder:

```txt
VITE_API_URL=http://127.0.0.1:8000
```
